@model SportsManager.Baseball.ViewModels.ManagersViewModel

<div class="panel panel-primary" id="teamManagers">
    <div class="panel-heading">
        <h3 class="panel-title" style="float: left;">Managers</h3>

        @if (Model.IsAdmin)
        {
            <button type="button" class="btn btn-default" style="float: right" data-bind="click: addManagerMode"><span class="glyphicon glyphicon-plus"></span></button>
        }

        <div class="clearfix"></div>
    </div>

    <div class="panel-body">
        @if (Model.IsAdmin)
        {
            <div class="row">
                <form style="display:none" data-bind="visible: !viewMode()">
                    <div class="form-group">
                        <div class="col-sm-12">
                            <div class="input-group">
                                <input type="text" class="form-control" data-bind="typeahead: { datasets: { source: getAvailableManagers, async: true, display: 'label', limit: 10 }, target: selectedPlayer, options: { highlight: true } }" placeholder="type manager last name" />
                                <div class="input-group-btn">
                                    <button class="btn btn-primary" data-bind="click: addManager, enable: isPlayerSelected"><span class="glyphicon glyphicon-ok"></span></button>
                                </div>
                            </div>
                        </div>
                    </div>
                </form>
            </div>
            <div class="row" style="display:none" data-bind="visible: !viewMode()">
                <div class="col-sm-12 help-block">
                    Manager must be on the teams roster to be added.
                </div>
            </div>
        }

        <div data-bind="foreach: managers">
            <div class="row" style="margin-bottom: 10px">
                <div class="col-sm-4">
                    <img onerror="this.style.display = 'none';" data-bind="attr: { src: Contact.PhotoURL }" class="img-rounded" />
                    @if (Model.IsAdmin)
                    {
                        <span data-bind="imageUploader: fileUploaderUrl" class="btn btn-default fileinput-button" title="upload player photo">
                            <i class="glyphicon glyphicon-upload"></i>
                            <input class="form-control" type="file" data-maxfilesize="1">
                        </span>
                    }
                </div>
                <div class="col-sm-8">
                    @if (Model.IsAdmin)
                    {
                        <button class="btn btn-warning pull-right" data-bind="click: $parent.removeManager"><span class="glyphicon glyphicon-minus"></span></button>
                    }
                    <address>
                        <strong><span data-bind="html: name"></span></strong><br />
                        @if (Model.IsAdmin || Model.IsTeamAdmin || Model.IsTeamMember)
                        { 
                            <!--ko if: Contact.Email -->
                            <a data-bind="attr: { href: 'mailto:' + Contact.Email()}">EMail</a><br />
                            <!--/ko-->
                            <!--ko if: Contact.Phone1 -->
                            <abbr title="Cell Phone">C:</abbr> <span data-bind="html: Contact.Phone1"></span><br />
                            <!--/ko-->
                            <!--ko if: Contact.Phone2 -->
                            <abbr title="Home Phone">H:</abbr> <span data-bind="html: Contact.Phone2"></span><br />
                            <!--/ko-->
                            <!--ko if: Contact.Phone3 -->
                            <abbr title="Work Phone">W:</abbr> <span data-bind="html: Contact.Phone3"></span><br />
                            <!--/ko-->
                        }
                    </address>
                </div>
            </div>
        </div>

        @if (Model.IsAdmin || Model.IsTeamAdmin)
        { 
            if (Model.ShowLineupCard)
            { 
                @Html.ActionLink("Lineup Card", "rostercard", "team", new { area = "baseball", accountId = Model.AccountId, id = Model.TeamSeasonId }, new { @class = "btn btn-info" })
            }
            <a href='@Url.Action("statistics", "team", new { accountId = Model.AccountId, Id = Model.TeamSeasonId })' class="btn btn-primary">Enter Stats</a>
        }
    </div>
</div>
