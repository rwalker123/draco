'use client';
import React from 'react';
import SocialHubTest from './SocialHubTest';

export default function SocialHubTestClientWrapper() {
  // If you need to extract accountId or other context, do it here (pattern for future expansion)
  return <SocialHubTest />;
}
